import React from "react";
import UserForm from "./UserForm";

const AddUser = () => {
    return (
        <React.Fragment>
            <UserForm uid={null} />
        </React.Fragment>
    );
};

export default AddUser;
